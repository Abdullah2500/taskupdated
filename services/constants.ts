const baseUrl = `http://stateview.morid.ca/api`;
export const REGISTER_URL = `${baseUrl}/register`;
export const LOGIN_URL = `${baseUrl}/login`;
export const COMMUNITY_URL = `${baseUrl}/community-list`;
